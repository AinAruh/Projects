import type { Asset } from '../../../types/asset'; import { StatusBadge } from './StatusBadge';
const typeLabels: Record<string,string> = { Motor:'Motor',Pump:'Bomba',Conveyor:'Esteira',Tank:'Tanque',Robot:'Robô',Machine:'Máquina',Other:'Outro' };
export function AssetDetails({ asset, onClose }: { asset: Asset; onClose:()=>void }) { const fields=[['Código',asset.code],['Nome',asset.name],['Tipo',typeLabels[asset.type]],['Fabricante',asset.manufacturer],['Modelo',asset.model],['Número de série',asset.serialNumber],['Cadastrado em',new Date(asset.createdAt).toLocaleString('pt-BR')]]; return <div className="p-6"><div className="mb-5"><StatusBadge status={asset.status}/></div><dl className="grid gap-5 sm:grid-cols-2">{fields.map(([label,value])=><div key={label}><dt className="text-xs font-semibold uppercase tracking-wide text-slate-400">{label}</dt><dd className="mt-1 text-sm font-medium text-slate-800">{value}</dd></div>)}<div className="sm:col-span-2"><dt className="text-xs font-semibold uppercase tracking-wide text-slate-400">Descrição</dt><dd className="mt-1 text-sm leading-6 text-slate-700">{asset.description || 'Sem descrição.'}</dd></div></dl><div className="mt-7 flex justify-end border-t border-slate-100 pt-5"><button onClick={onClose} className="rounded-lg bg-slate-800 px-5 py-2.5 text-sm font-semibold text-white hover:bg-slate-900">Fechar</button></div></div>; }
